# demo/Dockerfile.demo
# Extends the production API image — reuses ALL ML dependencies already built
# in catops-api:demo (PyTorch, torchvision, FastAPI, DVC, Poetry venv, etc.).
# Only adds the 3 demo-specific packages: streamlit, httpx, plotly.
#
# Build time: ~30 s (vs ~10 min rebuilding from scratch).
#
# Requires catops-api:demo to be built first.  The Makefile `demo` target
# handles this ordering explicitly.

FROM catops-api:demo

# Switch to root briefly to install the extra packages into the existing venv
USER root

RUN pip install --no-cache-dir \
    streamlit==1.45.0 \
    httpx==0.28.1 \
    plotly==6.1.1

# Drop back to the non-root user defined in the base image
USER appuser

EXPOSE 8501

ENV STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
ENV STREAMLIT_SERVER_HEADLESS=true

CMD ["streamlit", "run", "demo/streamlit_app.py", \
     "--server.port=8501", \
     "--server.address=0.0.0.0", \
     "--server.enableCORS=false", \
     "--server.enableXsrfProtection=false"]
